---
sidebar_position: 4
title: 持仓、自选与流水查询
sidebar_label: 4. 持仓与流水 (holding)
---

# 持仓、自选与流水查询

本章收录所有与个人账户相关的状态查询指令，涵盖自选关注池、账户持仓资产管理、自定义分组体系、交易买卖流水、待确认申赎队列、定投执行记录及每日盈亏追踪。所有本章内的命令均能够帮助开发者便捷地将账户投资组合实时对账状态接入个人财务仪表盘或智能分析平台。

---

## 4.1 `jgb holding` —— 持仓管理

`jgb holding` 是一组用于设置、查看和管理个人基金持仓的指令。支持查看全局或分组的持仓明细、实时估值状态、以及录入或清除持仓。

### 4.1.1 `jgb holding list` —— 查看持仓列表

#### 语法 (Synopsis)
```bash
jgb holding [list] [--group <group>] [--sort <field>] [--order <dir>] [--json] [--text]
```

#### 描述 (Description)
列出当前所有持仓基金及其份额、成本、持仓金额。系统会自动结合当时最新的实时盘中估值（或官方净值），动态计算各基金的当前持仓资产总值、持有盈亏与盈亏比例，并在汇总行展示账户总持仓与总盈亏。支持传入分组 ID 或名称进行分组持仓查询，并支持按指定字段排序展示。

#### 参数与选项 (Arguments & Options)
| 参数/选项 | 类型 | 是否必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `--group <group>` | string | 否 | 分组 ID 或分组名称，查看指定分组的持仓（如果分组名称存在多个相同名称，会报错提示使用分组 ID） |
| `--sort <field>` | string | 否 | 排序字段，支持多别名映射：`code`、`name`、`share`、`cost`、`amount`、`profit`、`rate`/`profitRate`、`group` |
| `--order <dir>` | string | 否 | 排序方向，支持 `desc`（降序，数值类指标默认）或 `asc`（升序，代码/名称/分组默认） |
| `--json` | boolean | `false` | 以结构化 JSON 格式输出结果 |
| `--text` | boolean | `false` | 以纯文本格式输出结果 |

#### 使用示例 (Examples)
```bash
# 查看全部持仓（默认执行 list）
jgb holding

# 查看全部持仓列表
jgb holding list

# 查看指定分组“养老定投”的持仓，并输出 JSON 数据
jgb holding list --group "养老定投" --json

# 按持有收益排序（数值指标默认降序）
jgb holding list --sort profit

# 按代码升序排列
jgb holding list --sort code --order asc
```

#### 输出格式 (Output)
**默认表格输出 (`table`)**：
```
$ jgb holding list

代码       名称                 份额        成本净值    持仓金额     持有收益     收益率     分组
────────────────────────────────────────────────────────────────────────────────────────────────────
110022    易方达消费行业股票    1000.00     1.5000      ¥2,680.00    +1180.00     +78.67%   全部
502056    广发中证医疗ETF联接A  148.21      0.6745      ¥87.21       -12.76       -12.77%   全部
001325    鹏华弘和混合A         30.31       1.6498      ¥62.41       +12.41       +24.82%   全部
002849    金信智能中国2025混合A  41.68       2.3993      ¥100.00      +0.00        +0.00%    哈哈哈

合计: 持仓金额 ¥4,091.38  总收益 +1,211.34  收益率 +42.06%
```

**JSON 结构化输出 (`--json`)**：
```json
{
  "ok": true,
  "command": "holding list",
  "timestamp": "2026-07-07T15:00:00.000Z",
  "data": {
    "holdings": [
      {
        "code": "110022",
        "name": "易方达消费行业股票",
        "share": 1000.00,
        "cost": 1.5000,
        "amount": 2680.00,
        "profit": 1180.00,
        "profitRate": 78.67,
        "group": "全部"
      },
      {
        "code": "002849",
        "name": "金信智能中国2025混合A",
        "share": 41.68,
        "cost": 2.3993,
        "amount": 100.00,
        "profit": 0.00,
        "profitRate": 0.00,
        "group": "哈哈哈"
      }
    ],
    "summary": {
      "totalAmount": 4091.38,
      "totalProfit": 1211.34,
      "profitRate": 42.06
    }
  }
}
```

**常规文本输出 (`--text`)**：
```
110022 易方达消费行业股票 份额:1000.00 成本:1.5000 金额:2680.00 收益:+1180.00 收益率:+78.67% 分组:全部
002849 金信智能中国2025混合A 份额:41.68 成本:2.3993 金额:100.00 收益:+0.00 收益率:+0.00% 分组:哈哈哈
合计: 持仓金额 4091.38 总收益 +1211.34 收益率 +42.06%
```

---

### 4.1.2 `jgb holding status` —— 查看持仓实时状态

#### 语法 (Synopsis)
```bash
jgb holding status [--sort <field>] [--order <dir>] [--json] [--text]
```

#### 描述 (Description)
获取资产组合的实时估算数据，包括最新估值、今日估算收益及累计盈亏比例。支持按指定字段排序展示。

#### 参数与选项 (Arguments & Options)
| 参数/选项 | 类型 | 是否必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `--sort <field>` | string | 否 | 排序字段，支持通用与模式特有别名映射：`code`、`name`、`share`/`todayShare`、`cost`/`costPrice`、`amount`/`todayValuation`、`profit`/`cumulativeProfitLoss`、`rate`/`profitRate`/`cumulativeProfitLossRatio`、`todayEarnings`/`today`/`earnings`（日内收益）、`gsz`/`nav`/`dwjz`（估算净值） |
| `--order <dir>` | string | 否 | 排序方向，支持 `desc`（降序，估值/盈亏等数值指标默认）或 `asc`（升序，代码/名称默认） |
| `--json` | boolean | `false` | 以 JSON 格式输出结构化报告 |
| `--text` | boolean | `false` | 以纯文本格式输出实时状态数据 |

#### 使用示例 (Examples)
```bash
# 查看表格形式实时估值状态
jgb holding status

# 按今日估算收益排序（数值默认降序）
jgb holding status --sort todayEarnings

# 按最新估算市值排序
jgb holding status --sort amount
```

#### 输出格式 (Output)
**默认表格输出 (`table`)**：
```
📊 资产组合实时状态报告 (2026-07-07)
-----------------------------------------------------------------------------------------
基金代码 | 基金名称             | 持有份额   | 成本价  | 估算净值 | 今日估算收益   | 累计盈亏 (率)       
-----------------------------------------------------------------------------------------
110022   | 易方达消费行业股票   | 1000.00    | 1.5000  | 1.5200   | +20.00 元      | +20.00 元 (+1.33%)  
-----------------------------------------------------------------------------------------
💰 全局统计指标:
  - 总市值: 1520.00 元
  - 总成本: 1500.00 元
  - 今日估算总收益: +20.00 元
  - 累计总盈亏: +20.00 元 (+1.33%)
-----------------------------------------------------------------------------------------
```

**JSON 结构化输出 (`--json`)**：
```json
{
  "ok": true,
  "command": "holding status",
  "timestamp": "2026-07-07T15:00:00.000Z",
  "data": {
    "holdings": [
      {
        "code": "110022",
        "name": "易方达消费行业股票",
        "share": 1000.00,
        "cost": 1.5000,
        "gsz": 1.5200,
        "todayProfit": 20.00,
        "totalProfit": 20.00,
        "profitRate": 1.33
      }
    ],
    "summary": {
      "totalValue": 1520.00,
      "totalCost": 1500.00,
      "todayProfit": 20.00,
      "totalProfit": 20.00,
      "profitRate": 1.33
    }
  }
}
```

**常规文本输出 (`--text`)**：
```
110022 易方达消费行业股票 份额:1000.00 成本价:1.5000 估算净值:1.5200 今日估算收益:+20.00 累计盈亏:+20.00 (+1.33%)
合计: 总市值 ¥1520.00 总成本 ¥1500.00 今日估算收益 +20.00 累计总盈亏 +20.00 (+1.33%)
```

---

## 4.2 `jgb watchlist list` —— 查询自选关注池

#### 语法 (Synopsis)
```bash
jgb watchlist [list] [--sort <field>] [--order <dir>] [--json] [--text]
```

#### 描述 (Description)
列出当前所有已加入收藏（自选）的基金及其单位净值、估算净值、估算涨跌幅和估值时间，帮助用户快速追踪潜在调仓备选标的的走势。支持按指定字段排序展示。

#### 参数与选项 (Arguments & Options)
| 参数/选项 | 类型 | 是否必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `--sort <field>` | string | 否 | 排序字段，支持多别名映射：`code`（基金代码）、`name`（基金名称）、`dwjz`/`nav`（单位净值）、`gsz`/`price`/`valuation`/`estimate`（实时估算净值）、`gszzl`/`rate`/`change`/`growth`（实时估算涨跌幅 %，[默认]）、`gztime`/`time`（估算时间） |
| `--order <dir>` | string | 否 | 排序方向，支持 `desc`（降序，数值字段默认）或 `asc`（升序，代码/名称默认） |
| `--json` | boolean | `false` | 以结构化 JSON 格式输出结果，同时禁用所有交互式 UI 动画与引导文字，适用于 jq、管道操作及程序化自动化处理 |
| `--text` | boolean | `false` | 以纯文本格式输出结果，去除控制台颜色和特殊 UI 符号，生成结构清晰的文字摘要，专为 LLM / AI Agent 优化消费 |

#### 使用示例 (Examples)
```bash
# 查看自选基金列表（默认执行 list）
jgb watchlist

# 查看自选基金列表
jgb watchlist list

# 按估算涨跌幅排序（默认降序）
jgb watchlist list --sort gszzl

# 按代码升序排列
jgb watchlist list --sort code --order asc

# 以 JSON 格式输出
jgb watchlist list --json
```

#### 输出格式 (Output)
**表格输出**：
```
$ jgb watchlist list

代码       名称                 单位净值    估算净值    估算涨幅     估值时间
───────────────────────────────────────────────────────────────────────────────────
005827    易方达蓝筹精选混合     1.2345      1.2400      +0.45%       2026-07-07 15:00
110022    易方达消费行业股票     1.5000      1.5100      +0.67%       2026-07-07 15:00
000001    华夏成长混合           1.1000      1.0900      -0.91%       2026-07-07 15:00
───────────────────────────────────────────────────────────────────────────────────
共 3 只自选基金
```

**JSON 结构化输出 (`--json`)**：
```json
{
  "ok": true,
  "command": "watchlist list",
  "timestamp": "2026-07-07T15:00:00.000Z",
  "data": [
    {
      "code": "005827",
      "name": "易方达蓝筹精选混合",
      "dwjz": 1.2345,
      "gsz": 1.24,
      "gszzl": 0.45,
      "gztime": "2026-07-07 15:00"
    },
    {
      "code": "110022",
      "name": "易方达消费行业股票",
      "dwjz": 1.5,
      "gsz": 1.51,
      "gszzl": 0.67,
      "gztime": "2026-07-07 15:00"
    }
  ],
  "meta": {
    "total": 2
  }
}
```

**常规文本输出 (`--text`)**：
```
005827 易方达蓝筹精选混合 单位净值:1.2345 估算净值:1.2400 估算涨幅:+0.45% 估值时间:2026-07-07 15:00
110022 易方达消费行业股票 单位净值:1.5000 估算净值:1.5100 估算涨幅:+0.67% 估值时间:2026-07-07 15:00
```

---

## 4.3 `jgb group list` —— 查询分组体系与成员

#### 语法 (Synopsis)
```bash
jgb group list [options]
```

#### 描述 (Description)
列出当前账户定义的所有自定义分类分组（如“长期持有”、“宽基指数”、“短线网格”等），并汇总各分组下包含的标的数量以及各个分组在持仓资产中的占比结构。传入 `--with-members` 可在输出中展开各分组的详细成员名单，同时展示每只成员基金的代码与完整名称。

#### 参数与选项 (Arguments & Options)
| 参数/选项 | 类型 | 是否必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `--type <type>`  | string | 否 | 过滤分组归属：`holding`（持仓分组）、`watchlist`（自选分组）、`all`（默认） |
| `--with-members` | flag   | 否 | 在输出中展开列示各分组所包含的全部详细成员基金，同时展示基金代码与完整名称 |
| `--json` | boolean | `false` | 以结构化 JSON 格式输出结果，同时禁用所有交互式 UI 动画与引导文字，适用于 jq、管道操作及程序化自动化处理 |
| `--text` | boolean | `false` | 以纯文本格式输出结果，去除控制台颜色和特殊 UI 符号，生成结构清晰的文字摘要，专为 LLM / AI Agent 优化消费 |

#### 使用示例 (Examples)
```bash
# 查看所有分组结构并展开成员名单（含基金名称）
jgb group list --with-members

# 仅查看持仓类型的分组
jgb group list --type holding --with-members

# 以 JSON 格式输出，便于程序化消费
jgb group list --with-members --json
```

#### 输出格式 (Output)
**表格输出**：
```
$ jgb group list --with-members

分组名称     类型       包含基金数   持仓市值       资产占比   成员明细
──────────────────────────────────────────────────────────────────────────────────────────────────────
长期持有     持仓分组   2            ¥105,200.00    66.39%     005827 易方达蓝筹精选混合；012345 半导体先锋股票A
科技前沿     持仓分组   1            ¥53,250.00     33.61%     054321 人工智能精选混合C
观察自选     自选分组   4            --             --         016665 纳斯达克100 ETF联接；000001 华夏成长混合；005818 易方达蓝筹精选混合；110011 易方达中小盘混合
```

**JSON 结构化输出 (`--json`)**：
```json
{
  "ok": true,
  "command": "group list",
  "timestamp": "2026-07-07T15:00:00.000Z",
  "data": [
    {
      "groupId": "grp-01",
      "name": "长期持有",
      "type": "holding",
      "fundCount": 2,
      "totalValue": 105200.00,
      "weight": 66.39,
      "members": [
        { "code": "005827", "name": "易方达蓝筹精选混合" },
        { "code": "012345", "name": "半导体先锋股票 A" }
      ]
    }
  ]
}
```

**常规文本输出 (`--text`)**：
```
分组: 长期持有 (持仓分组) 基金数:2 持仓市值:¥105,200.00 占比:66.39%
  - 005827 易方达蓝筹精选混合
  - 012345 半导体先锋股票A
分组: 科技前沿 (持仓分组) 基金数:1 持仓市值:¥53,250.00 占比:33.61%
  - 054321 人工智能精选混合C
分组: 观察自选 (自选分组) 基金数:4
  - 016665 纳斯达克100 ETF联接
  - 000001 华夏成长混合
  - 005818 易方达蓝筹精选混合
  - 110011 易方达中小盘混合
```

---

## 4.4 `jgb tag list` —— 标签查询与筛选

#### 语法 (Synopsis)
```bash
jgb tag list [keyword] [--with-members] [--json] [--text]
```

#### 描述 (Description)
查看账户中所有自定义标签（含名称、颜色主题及关联基金数量）。支持传入关键词按标签名称进行模糊搜索，快速定位目标标签。传入 `--with-members` 可在输出中展开各标签的全部关联成员基金，同时展示基金代码与完整名称。

#### 参数与选项 (Arguments & Options)
| 参数/选项 | 类型 | 是否必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `[keyword]` | string | 否 | 按标签名称模糊搜索的关键词，不传则列出全部标签 |
| `--with-members` | flag | 否 | 在输出中展开列示各标签所包含的全部关联成员基金，同时展示基金代码与完整名称 |
| `--json` | boolean | `false` | 以结构化 JSON 格式输出结果，同时禁用所有交互式 UI 动画与引导文字，适用于 jq、管道操作及程序化自动化处理 |
| `--text` | boolean | `false` | 以纯文本格式输出结果，去除控制台颜色和特殊 UI 符号，生成结构清晰的文字摘要，专为 LLM / AI Agent 优化消费 |

#### 使用示例 (Examples)
```bash
# 列出系统内全部自定义标签
jgb tag list

# 按名称关键词模糊搜索标签
jgb tag list 科技

# 查看所有标签并展开关联成员基金名单
jgb tag list --with-members

# 模糊搜索并展开成员，以 JSON 格式输出
jgb tag list 科技 --with-members --json
```

#### 输出格式 (Output)

**默认表格输出**：
```
$ jgb tag list

ID                    名称        主题      关联基金数
──────────────────────────────────────────────────────
tag-a1b2c3d4          科技        蓝色      3
tag-e5f6g7h8          消费        绿色      2
tag-i9j0k1l2          医药        红色      4
```

**`--with-members` 表格输出**：
```
$ jgb tag list --with-members

ID                    名称        主题      关联基金数   成员明细
──────────────────────────────────────────────────────────────────────────────────────────────────────
tag-a1b2c3d4          科技        蓝色      3            005827 易方达蓝筹精选混合；260108 某半导体混合A；012345 某人工智能ETF联接A
tag-e5f6g7h8          消费        绿色      2            110022 易方达消费行业股票；005827 易方达蓝筹精选混合
tag-i9j0k1l2          医药        红色      4            005827 易方达蓝筹精选混合；502056 广发中证医疗ETF联接A；001325 鹏华弘和混合A；002849 金信智能中国2025混合A
```

**JSON 结构化输出 (`--json`)**：
```json
{
  "ok": true,
  "command": "tag list",
  "timestamp": "2026-07-07T15:00:00.000Z",
  "data": [
    {
      "id": "tag-a1b2c3d4",
      "name": "科技",
      "theme": "blue",
      "fundCount": 3,
      "members": [
        { "code": "005827", "name": "易方达蓝筹精选混合" },
        { "code": "260108", "name": "某半导体混合A" },
        { "code": "012345", "name": "某人工智能ETF联接A" }
      ]
    },
    {
      "id": "tag-e5f6g7h8",
      "name": "消费",
      "theme": "green",
      "fundCount": 2,
      "members": [
        { "code": "110022", "name": "易方达消费行业股票" },
        { "code": "005827", "name": "易方达蓝筹精选混合" }
      ]
    }
  ]
}
```

**常规文本输出 (`--text`)**：
```
标签: 科技 [蓝色] 关联基金数:3
  - 005827 易方达蓝筹精选混合
  - 260108 某半导体混合A
  - 012345 某人工智能ETF联接A
标签: 消费 [绿色] 关联基金数:2
  - 110022 易方达消费行业股票
  - 005827 易方达蓝筹精选混合
```

---

## 4.5 `jgb transactions` —— 交易买卖流水

#### 语法 (Synopsis)
```bash
jgb transactions <code> [--group <group>] [--json] [--text]
```

#### 描述 (Description)
列出指定基金的历史交易记录，展示每笔交易的类型、发生金额、成交份额、成交价格及交易日期等关键信息。支持传入分组 ID 或唯一分组名称，按分组筛选指定基金的交易记录。

#### 参数与选项 (Arguments & Options)
| 参数/选项 | 类型 | 是否必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `<code>` | string | 是 | 基金代码，指定要查询交易记录的基金 |
| `--group <group>` | string | 否 | 按分组 ID 或唯一分组名称筛选交易记录 |
| `--json` | boolean | `false` | 以结构化 JSON 格式输出结果，同时禁用所有交互式 UI 动画与引导文字，适用于 jq、管道操作及程序化自动化处理 |
| `--text` | boolean | `false` | 以纯文本格式输出结果，去除控制台颜色和特殊 UI 符号，生成结构清晰的文字摘要，专为 LLM / AI Agent 优化消费 |

#### 使用示例 (Examples)
```bash
# 查看指定基金的交易记录
jgb transactions 005827

# 按分组筛选指定基金的交易记录
jgb transactions 005827 --group 长期持有

# 以 JSON 格式输出交易记录
jgb transactions 005827 --json

# 以纯文本格式输出交易记录
jgb transactions 005827 --text
```

#### 输出格式 (Output)
**表格输出**：
```
$ jgb transactions 005827

📋 基金 005827 的交易记录:
----------------------------------------------------------------------
ID      | 类型 | 金额 (元)  | 份额     | 价格    | 日期       | 时间戳
----------------------------------------------------------------------
abc123  | 买入 | 10000      | 6544.10  | 1.5258  | 2026-06-18 | 1718505000000
def456  | 卖出 | 152.58     | 100.00   | 1.5258  | 2026-06-18 | 1718505000000
----------------------------------------------------------------------
```

**JSON 结构化输出 (`--json`)**：
```json
{
  "ok": true,
  "command": "transactions",
  "data": [
    {
      "id": "abc123",
      "type": "buy",
      "amount": 10000,
      "share": 6544.10,
      "price": 1.5258,
      "date": "2026-06-18",
      "timestamp": 1718505000000
    }
  ],
  "meta": { "total": 2 }
}
```

**常规文本输出 (`--text`)**：
```
2026-06-18 买入 金额:10000 份额:6544.10 价格:1.5258
2026-06-18 卖出 金额:152.58 份额:100.00 价格:1.5258
```

---

## 4.6 `jgb pending list` —— 待处理交易队列

#### 语法 (Synopsis)
```bash
jgb pending list [--code <code>] [--json] [--text]
```

#### 描述 (Description)
当交易提交时若最新净值尚未更新（如在非交易时段提交交易），该笔交易将自动进入待处理队列。本指令用于查询当前所有处于待处理状态的交易流水，待净值更新后系统会自动完成处理。支持按基金代码筛选指定基金的待处理交易。

#### 参数与选项 (Arguments & Options)
| 参数/选项 | 类型 | 是否必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `--code <code>` | string | 否 | 按基金代码筛选待处理交易 |
| `--json` | boolean | `false` | 以结构化 JSON 格式输出结果，同时禁用所有交互式 UI 动画与引导文字，适用于 jq、管道操作及程序化自动化处理 |
| `--text` | boolean | `false` | 以纯文本格式输出结果，去除控制台颜色和特殊 UI 符号，生成结构清晰的文字摘要，专为 LLM / AI Agent 优化消费 |

#### 使用示例 (Examples)
```bash
# 查看全部待处理交易
jgb pending list

# 按基金代码筛选待处理交易
jgb pending list --code 005827

# 以 JSON 格式输出
jgb pending list --json

# 以纯文本格式输出
jgb pending list --text
```

#### 输出格式 (Output)
**默认表格输出 (`table`)**：
```
$ jgb pending list

ID                  基金代码  名称                  类型    金额/份额     日期                  状态
────────────────────────────────────────────────────────────────────────────────────────────────────────
tx-e5f6g7h8         005827    易方达蓝筹精选混合    买入    10000.00元    2026-06-15            等待净值
tx-i9j0k1l2         000001    易方达价值成长混合    卖出    100.00份      2026-06-14 (15:00后)  等待净值
────────────────────────────────────────────────────────────────────────────────────────────────────────

共 2 笔待处理交易
```

**JSON 结构化输出 (`--json`)**：
```json
{
  "ok": true,
  "command": "pending list",
  "data": [
    {
      "id": "tx-e5f6g7h8",
      "fundCode": "005827",
      "fundName": "易方达蓝筹精选混合",
      "type": "buy",
      "amount": 10000,
      "date": "2026-06-15",
      "isAfter3pm": false,
      "isDca": false,
      "timestamp": 1718400000000
    },
    {
      "id": "tx-i9j0k1l2",
      "fundCode": "000001",
      "fundName": "易方达价值成长混合",
      "type": "sell",
      "share": 100,
      "date": "2026-06-14",
      "isAfter3pm": true,
      "isDca": false,
      "timestamp": 1718313600000
    }
  ],
  "meta": {
    "total": 2
  }
}
```

**常规文本输出 (`--text`)**：
```
tx-e5f6g7h8 005827 易方达蓝筹精选混合 买入 金额:10000.00元 日期:2026-06-15 状态:等待净值
tx-i9j0k1l2 000001 易方达价值成长混合 卖出 份额:100.00份 日期:2026-06-14 15:00后 状态:等待净值
```

---

## 4.7 `jgb dca list` —— 智能定投计划查询

#### 语法 (Synopsis)
```bash
jgb dca list [options]
```

#### 描述 (Description)
查询当前账户所有已建立定投配置（Dollar-Cost Averaging, DCA）计划的概览状态，包括定投周期（按日/按周/按月）及对应扣款日、每期定额扣款金额、申购费率、起始扣款日期、计划所属分组（全局或指定分组）、以及计划当前是否处于运行状态。定投计划可挂载于全局（`__global__`）或某个自定义分组（`group_<id>`）之下，查询时支持按分组筛选。

#### 参数与选项 (Arguments & Options)
| 参数/选项 | 类型 | 是否必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `--status <state>`| string| 否 | 过滤状态：`active`（运行中[默认]）、`paused`（已暂停）、`all`（全部） |
| `--group <group>` | string | 否 | 分组 ID 或分组名称，仅查看指定分组下的定投计划；不传则汇总全部（含全局与各分组） |
| `--json` | boolean | `false` | 以结构化 JSON 格式输出结果，同时禁用所有交互式 UI 动画与引导文字，适用于 jq、管道操作及程序化自动化处理 |
| `--text` | boolean | `false` | 以纯文本格式输出结果，去除控制台颜色和特殊 UI 符号，生成结构清晰的文字摘要，专为 LLM / AI Agent 优化消费 |

#### 使用示例 (Examples)
```bash
# 查看全部定投计划（默认仅显示运行中）
jgb dca list

# 查看包含已暂停的全部定投计划
jgb dca list --status all

# 仅查看指定分组"哈哈哈"下的定投计划，并输出 JSON
jgb dca list --group "哈哈哈" --json
```

#### 输出格式 (Output)
**表格输出**：
```
$ jgb dca list

基金代码   基金名称                扣款周期     每期金额     费率     起始日期     分组       状态
──────────────────────────────────────────────────────────────────────────────────────────────
002849    金信智能中国2025混合A    每日         ¥10.00      0.00%    2026-07-08   哈哈哈     运行中
005827    易方达蓝筹精选混合       每周三       ¥1,000.00   0.00%    2026-07-02   长期持有   运行中
006444    招商中证白酒指数A        每月7日      ¥100.00     0.00%    2026-07-07   全局       运行中

共 3 条定投计划 (运行中 3 / 已暂停 0)
```

**JSON 结构化输出 (`--json`)**：
```json
{
  "ok": true,
  "command": "dca list",
  "timestamp": "2026-07-07T15:00:00.000Z",
  "data": [
    {
      "code": "002849",
      "name": "金信智能中国2025混合A",
      "cycle": "daily",
      "amount": 10,
      "feeRate": 0,
      "firstDate": "2026-07-08",
      "weeklyDay": null,
      "monthlyDay": null,
      "enabled": true,
      "scope": "group_1777207435952",
      "groupName": "哈哈哈"
    },
    {
      "code": "005827",
      "name": "易方达蓝筹精选混合",
      "cycle": "weekly",
      "amount": 1000,
      "feeRate": 0,
      "firstDate": "2026-07-02",
      "weeklyDay": 3,
      "monthlyDay": null,
      "enabled": true,
      "scope": "group_1777207435952",
      "groupName": "长期持有"
    },
    {
      "code": "006444",
      "name": "招商中证白酒指数A",
      "cycle": "monthly",
      "amount": 100,
      "feeRate": 0,
      "firstDate": "2026-07-07",
      "weeklyDay": null,
      "monthlyDay": 7,
      "enabled": true,
      "scope": "__global__",
      "groupName": "全局"
    }
  ],
  "meta": {
    "total": 3,
    "active": 3,
    "paused": 0
  }
}
```

**常规文本输出 (`--text`)**：
```
002849 金信智能中国2025混合A 周期:每日 金额:10.00 费率:0.00% 起始:2026-07-08 分组:哈哈哈 状态:运行中
005827 易方达蓝筹精选混合 周期:每周三 金额:1000.00 费率:0.00% 起始:2026-07-02 分组:长期持有 状态:运行中
006444 招商中证白酒指数A 周期:每月7日 金额:100.00 费率:0.00% 起始:2026-07-07 分组:全局 状态:运行中
共 3 条定投计划
```

---

## 4.8 `jgb earnings` —— 每日盈亏与账户收益统计

#### 语法 (Synopsis)
```bash
jgb earnings [code] [options]
```

#### 描述 (Description)
查询指定基金或整个账户在历史日期范围内的每日浮动盈亏记录，展示日盈亏金额、日盈亏百分比及当日账户总资产快照，帮助用户清晰追踪特定日期市场波动对个人资金盘造成的绝对数额影响。支持传入基金代码查看单基金日盈亏，或查看全账户持仓汇总日盈亏；支持通过 `--days` 指定回溯交易日天数或通过 `--from` 明确指定起始追踪日期。

#### 参数与选项 (Arguments & Options)
| 参数/选项 | 类型 | 是否必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `[code]` | string | 否 | 传对应基金代码则查看单基金日盈亏；不传或传 `all` 则看全账户持仓汇总日盈亏 |
| `--days <num>`    | number| 否 | 查看最近的连续多少个交易日记录，默认值 `7` |
| `--from <date>`   | string| 否 | 明确指定起始追踪日期 |
| `--json` | boolean | `false` | 以结构化 JSON 格式输出结果，同时禁用所有交互式 UI 动画与引导文字，适用于 jq、管道操作及程序化自动化处理 |
| `--text` | boolean | `false` | 以纯文本格式输出结果，去除控制台颜色和特殊 UI 符号，生成结构清晰的文字摘要，专为 LLM / AI Agent 优化消费 |

#### 使用示例 (Examples)
```bash
# 查看全账户最近 5 个交易日的总盈亏变化
jgb earnings --days 5

# 查看特定重仓基金在近 1 个月的日利变动，输出 JSON 供归因分析
jgb earnings 005827 --days 30 --json
```

#### 输出格式 (Output)
**全账户汇总表格**：
```
$ jgb earnings --days 5

账户每日盈亏追踪 (近 5 个交易日)
日期         账户总资产     当日发生盈亏额 (元)   当日总资产收益率
──────────────────────────────────────────────────────────────────
2026-07-03   ¥158,450.00    +¥1,250.00           +0.80%
2026-07-02   ¥157,200.00    -¥820.00             -0.52%
2026-07-01   ¥158,020.00    +¥2,100.00           +1.35%
2026-06-30   ¥155,920.00    +¥450.00             +0.29%
2026-06-29   ¥155,470.00    -¥310.00             -0.20%
```
**JSON 结构化输出 (`--json`)**：
```json
{
  "ok": true,
  "command": "earnings",
  "timestamp": "2026-07-07T15:00:00.000Z",
  "data": {
    "scope": "account_total",
    "days": 5,
    "records": [
      {
        "date": "2026-07-03",
        "totalAssets": 158450.00,
        "dailyProfit": 1250.00,
        "dailyProfitPercent": 0.80
      }
    ]
  }
}
```

**常规文本输出 (`--text`)**：
```
2026-07-03 账户总资产:¥158,450.00 当日盈亏:+¥1,250.00 收益率:+0.80%
2026-07-02 账户总资产:¥157,200.00 当日盈亏:-¥820.00 收益率:-0.52%
2026-07-01 账户总资产:¥158,020.00 当日盈亏:+¥2,100.00 收益率:+1.35%
2026-06-30 账户总资产:¥155,920.00 当日盈亏:+¥450.00 收益率:+0.29%
2026-06-29 账户总资产:¥155,470.00 当日盈亏:-¥310.00 收益率:-0.20%
```
